# Normalize line endings to LF in the repository (consistent cross-platform diffs).
* text=auto eol=lf

# Binary / generated lockfiles — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
