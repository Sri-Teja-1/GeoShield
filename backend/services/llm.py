"""LLM wrapper (Gemini Flash).

Phase 2: ``classify_structured(prompt: str) -> dict`` using
``google.generativeai`` with Gemini Flash in JSON mode, and ``synthesize(prompt:
str) -> str`` for free-text synthesis calls.
"""
